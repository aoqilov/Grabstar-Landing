import React from "react";
import "../../assets/style/UI component/GreenBtn.scss";

const GreenBtn = ({ classNames }) => {
  return (
    <>
      <button>{classNames}</button>
    </>
  );
};

export default GreenBtn;
